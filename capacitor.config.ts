import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.qinglun.subtitlesync',
  appName: '音频字幕同步器',
  webDir: 'build'
};

export default config;
